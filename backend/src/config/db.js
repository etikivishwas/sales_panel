const fs=require("fs"),path=require("path"),mysql=require("mysql2/promise");const p=process.env.DB_SSL_CA?path.resolve(process.cwd(),process.env.DB_SSL_CA):null,ssl={minVersion:"TLSv1.2",rejectUnauthorized:true};if(p){if(!fs.existsSync(p))throw new Error(`CA not found: ${p}`);ssl.ca=fs.readFileSync(p,"utf8")}const pool=mysql.createPool({host:process.env.DB_HOST,user:process.env.DB_USER,password:process.env.DB_PASSWORD,database:process.env.DB_NAME,port:Number(process.env.DB_PORT)||4000,ssl,waitForConnections:true,connectionLimit:10,decimalNumbers:true,timezone:"Z"});async function testConnection(){const [r]=await pool.query("SELECT DATABASE() db,VERSION() version");console.log("Database connected:",r[0])}module.exports={pool,testConnection};
