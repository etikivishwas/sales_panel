const jwt=require("jsonwebtoken");module.exports=(req,res,next)=>{const h=req.header("authorization")||"";if(!h.startsWith("Bearer "))return res.status(401).json({success:false,message:"Authentication token is required"});try{const p=jwt.verify(h.slice(7),process.env.JWT_SECRET);req.salesExecutive={id:Number(p.sub)};next()}catch(e){res.status(401).json({success:false,message:"Invalid or expired authentication token"})}};
